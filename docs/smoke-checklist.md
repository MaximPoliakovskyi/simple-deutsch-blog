# Manual Smoke Checklist

- [ ] `/` redirects to `/en`
- [ ] `/en`, `/ru`, `/uk` load
- [ ] `/en/posts` loads
- [ ] `/en/posts/[slug]` loads
- [ ] Language switching works only in the top Navigation dropdown
- [ ] Footer has no language switch UI
- [ ] `cd web && npm run check` passes
